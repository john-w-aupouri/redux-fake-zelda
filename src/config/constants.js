// Size of Character
export const SPRITE_SIZE = 40
// OUT OF BOUNDS variables
export const MAP_HEIGHT = SPRITE_SIZE * 10 // because 10 * 40 = 400 which is the height of the map.
export const MAP_WIDTH = SPRITE_SIZE * 20 // because 20 * 40 = 400 which is the width of the map.